import * as React from 'react'
import * as ReactDOM from 'react-dom'
import MacroCopy from './macro_copy_app'

const macro_copy = document.getElementById('macro-copy');

ReactDOM.render(<MacroCopy />, macro_copy)